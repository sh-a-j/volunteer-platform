# This file is used during Docker build of the frontend.
# When running inside Docker, nginx proxies /api/* to the backend container.
# So the frontend calls /api/... instead of http://localhost:8080
VITE_API_URL=/api
